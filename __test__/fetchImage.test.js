import { fetchImage } from '../src/client/js/app.js'

describe("Testing if fetchImage is defined", () => {
    test("Testing the fetchImage function", () => {
           expect(fetchImage).toBeDefined();
})});